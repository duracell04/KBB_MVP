"""
Matching engine (placeholder)

Intention:
- Join bank refs (MsgId/UETR) with on-chain event metadata (SubscriptionSettled, CouponPaid).
Why:
- Deterministic reconciliation reports (matched, under/over, duplicates) for auditors.
"""
if __name__ == "__main__":
    print("placeholder matcher — wire real logic later")
