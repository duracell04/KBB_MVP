/**
 * ISO 20022 adapter (placeholder)
 * Intention: Parse camt.053/054 XML into normalized JSON (amount, currency, valueDate, MsgId/UETR).
 * Why: Provide deterministic reconciliation inputs for the orchestrator and on-chain events.
 */
export {}
