#
# Copy this file to .env.local to override runtime values without committing secrets.
#
# Next dev server port. The scripts default to 3000 but you can pick any free port.
PORT=3000

# Example public config entries – keep keys consistent between local + preview.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api
